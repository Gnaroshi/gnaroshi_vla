# Agent Context

Architecture-specific agent guidance is kept here instead of inside
`architectures/<arch>/`.

The root `AGENTS.md` remains at repository root so agents can discover the
global workspace rules quickly.
