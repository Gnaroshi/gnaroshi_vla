# Architecture Integration Docs

This directory stores metadata about how upstream architecture sources were
integrated into `gnaroshi_vla`.

Use `docs/architectures/<architecture>/` for manifests, modified-file notes,
source snapshots, and other non-runtime integration records.
